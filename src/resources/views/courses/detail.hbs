<div class="mt-4">
  <div class="row">
    <div class="col-lg-3">
      <button>Học Ngay</button>
    </div>
    <div class="col-lg-9">
      <h2 class="name">{{course.name}}</h2>
      <img src="{{course.image}}" alt="" class="w-50" />
      <p class="desc">{{course.description}}</p>
    </div>
  </div>
</div>